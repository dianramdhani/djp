require('./tr-loading.css');

(function () {
    'use strict';

    // Usage:
    // Loading view.

    window.app
        .component('trLoading', {
            template: require('./tr-loading.html'),
        });
})();